---
declared-services:
  conversation-simple-ng:
    label: conversation
    plan: free
applications:
- name: conversation-simple-ng
  command: npm start
  path: .
  memory: 256M
  instances: 1
  services:
  - conversation-simple-ng
  env:
    NPM_CONFIG_PRODUCTION: false
